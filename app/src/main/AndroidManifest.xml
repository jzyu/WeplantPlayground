<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jzyu.weplantplayground">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ToolbarHiddenOfScrollviewActivity" />
        <activity android:name=".GestureNormalActivity" />
        <activity android:name=".GestureScrollViewActivity" />
        <activity android:name=".PtrBubbleMsgActivity"></activity>
    </application>

</manifest>